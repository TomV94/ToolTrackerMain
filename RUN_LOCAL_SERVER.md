# Run Tool Tracker Backend (Server) Locally

Copy and paste each command into your terminal:

```sh
cd tool-tracker-app/server
npm install
npm start
```

- The backend will run on http://localhost:5000 