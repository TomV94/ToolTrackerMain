# Run Tool Tracker Frontend (Client) Locally

Copy and paste each command into your terminal:

```sh
cd tool-tracker-app/client
npm install
npm run dev
```

- The frontend will run on http://localhost:5173 